# There's no point limiting ourselves to Debian's outdated versions.
# It probably won't have a complete set of useful dependencies.
Flask==0.12.2
marshmallow==2.13.5

# Dependencies.
click==6.7
itsdangerous==0.24
Jinja2==2.9.6
MarkupSafe==1.0
Werkzeug==0.12.2
